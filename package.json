{
  "name": "db",
  "version": "1.0.0",
  "description": "this is a Mongodb module with basic functions such as a singleton connection, findAll, edit, and delete queries",
  "main": "dist/index.js",
  "types": "dist/index.d.ts",
  "scripts": {
    "test": "echo \"Error: no test specified\" && exit 1",
    "build": "npx run build",
    "start": "node dist/index.js"
  },
  "author": "shimi yadan",
  "license": "ISC",
  "devDependencies": {
    "@types/node": "^20.4.2",
    "dotenv": "^16.3.1",
    "nodemon": "^3.0.1",
    "typescript": "^5.1.6"
  },
  "dependencies": {
    "mongodb": "^5.7.0"
  }
}
